export type RadioServiceConfiguration = { boombox: { textureId: string } }

export type CloudServiceConfiguration = { apiKey: string, appId: string, apiServer: string }

export type PurchaseServiceConfiguration = { appId: string, purchase: { string: { string: { string } } } }

return {}
